// import { connect } from "react-redux";

// const mSTP = (state) => {
//   return {};
// };

// const mDTP = (dispatch) => ({
//   fetchEvent: (eventId) => dispatch(fetchEvent(eventId)),
// });

// export default connect(
//   mSTP,
//   mDTP
// )(EventCard);
